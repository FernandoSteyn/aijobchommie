{
  "name": "aijobchommie",
  "version": "1.0.0",
  "description": "AI Job Chommie - Smart job search assistant for South Africa",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm ci --only=production --no-audit --no-fund",
    "dev": "cd backend && npm run dev",
    "postinstall": "cd backend && npm ci --only=production --no-audit --no-fund"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": ["job-search", "ai", "south-africa", "pwa"],
  "author": "Fernando Steyn",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
